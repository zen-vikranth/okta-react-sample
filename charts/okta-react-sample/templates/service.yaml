apiVersion: v1
kind: Service
metadata:
  name: {{ include "okta-react-sample.fullname" . }}
  labels:
    app: {{ include "okta-react-sample.name" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80
  selector:
    app: {{ include "okta-react-sample.name" . }}
